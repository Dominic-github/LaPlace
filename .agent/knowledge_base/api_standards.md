# 🌐 API Standards - LaPlace

> Auto-generated by WB-Agent Scanner → Enriched with actual data

## Conventions
- Base URL: `/api/` (không có v1 prefix)
- Authentication: Bearer Token (JWT)
- Response format: `{ success: boolean, data: any, meta?: { total, page, limit, totalPages } }`
- Error format: `{ success: false, message: string }`
- Pagination: `?page=1&limit=10`
- Sorting: `?sort=createdAt&order=DESC`

## Route Modules (8)

| Module | Base Path | Controller | Auth Required |
|--------|-----------|------------|---------------|
| Auth | `/api/auth/*` | auth.js | Partial |
| Accommodations | `/api/accommodations/*` | accommodation.js | Partial |
| Health | `/api/health` | health | No |
| Payments | `/api/payments/*` | payment.js | Yes |
| Permissions | `/api/permissions/*` | permission.js | Yes (Admin) |
| Roles | `/api/roles/*` | role.js | Yes (Admin) |
| Users | `/api/users/*` | user.js | Yes (Admin) |
| Messages | `/api/messages/*` | message.js | Yes |

## Auth Endpoints

| Method | Path | Description | Auth |
|--------|------|-------------|------|
| POST | `/api/auth/register` | Đăng ký tài khoản | No |
| POST | `/api/auth/login` | Đăng nhập | No |
| POST | `/api/auth/logout` | Đăng xuất | Yes |
| GET | `/api/auth/me` | Lấy thông tin user | Yes |
| POST | `/api/auth/refresh` | Refresh token | Yes |

## Accommodation Endpoints

| Method | Path | Description | Auth |
|--------|------|-------------|------|
| GET | `/api/accommodations` | Danh sách (filter, search, pagination) | No |
| GET | `/api/accommodations/:id` | Chi tiết | No |
| POST | `/api/accommodations` | Tạo mới | Admin |
| PUT | `/api/accommodations/:id` | Cập nhật | Admin |
| DELETE | `/api/accommodations/:id` | Xóa | Admin |

## HTTP Status Codes

| Code | Usage |
|------|-------|
| 200 | Success |
| 201 | Created |
| 400 | Bad Request (validation) |
| 401 | Unauthorized (no/invalid token) |
| 403 | Forbidden (insufficient role) |
| 404 | Not Found |
| 500 | Internal Server Error |

## Middleware Stack

```
Request → CORS → Body Parser → Auth → Role Check → Controller → Response
```

Xem chi tiết: `docs/server.md`