# 📊 Data Schema - LaPlace

> Auto-generated by WB-Agent Scanner → Enriched with actual data

## Database
- **Engine**: MySQL 8.0
- **ORM**: Sequelize 6.37.7
- **Models Path**: `server/src/models/`
- **Migrations**: `server/src/migrations/` (19 files)
- **Seeders**: `server/src/seeders/` (4 files)

## Models (19)

### Auth & Users (7)
| Model | Table | Key Fields |
|-------|-------|------------|
| User | `users` | id, firstName, lastName, email, password, phone, avatar, status |
| Role | `roles` | id, name, description |
| Permission | `permissions` | id, resource, action, description |
| UserRole | `user_roles` | userId, roleId (junction) |
| RolePermission | `role_permissions` | roleId, permissionId (junction) |
| Token | `tokens` | id, userId, token, type, expiresAt |
| RefreshTokenUsed | `refresh_tokens_used` | id, userId, token |

### Accommodations (4)
| Model | Table | Key Fields |
|-------|-------|------------|
| Accommodation | `accommodations` | id, title, description, address, province, district, ward, price, area, bedrooms, bathrooms, maxOccupancy, status, images, featured |
| AccommodationImage | `accommodation_images` | id, accommodationId, url, order |
| Facility | `facilities` | id, name, icon, category |
| AccommodationFacility | `accommodation_facilities` | accommodationId, facilityId (junction) |

### Business (2)
| Model | Table | Key Fields |
|-------|-------|------------|
| Booking | `bookings` | id, userId, accommodationId, checkInDate, checkOutDate, guests, totalPrice, status, notes |
| Contract | `contracts` | id, bookingId, userId, accommodationId, startDate, endDate, monthlyRent, deposit, terms, contractFile, status |

### Payments & Reviews (2)
| Model | Table | Key Fields |
|-------|-------|------------|
| Payment | `payments` | id, bookingId, contractId, userId, amount, paymentMethod, status, transactionId, paidAt |
| Review | `reviews` | id, userId, accommodationId, rating, comment, images, isApproved |

### Social (2)
| Model | Table | Key Fields |
|-------|-------|------------|
| Favorite | `favorites` | id, userId, accommodationId |
| Message | `messages` | id, senderId, receiverId, content, read |

### System (1)
| Model | Table | Key Fields |
|-------|-------|------------|
| Report | `reports` | id, type, data, period |

## Key Enums

```
Accommodation.status: available | occupied | maintenance
Accommodation.type: phong_tro | can_ho | nha_nguyen_can | ky_tuc_xa
Booking.status: pending | confirmed | completed | cancelled
Contract.status: active | expired | terminated
Payment.status: pending | completed | failed
Payment.method: cash | bank_transfer | online
User.status: active | inactive | banned
```

## Relationships

```
User (1) ──→ (N) Booking, Review, Contract, Favorite, Message, Token
Accommodation (1) ──→ (N) Booking, Review, AccommodationImage
Accommodation (N) ←──→ (N) Facility (through AccommodationFacility)
Booking (1) ──→ (1) Contract
Contract (1) ──→ (N) Payment
User (N) ←──→ (N) Role (through UserRole)
Role (N) ←──→ (N) Permission (through RolePermission)
```

Xem chi tiết: `.agent/knowledge_base/database_schema.md`, `docs/server.md`
