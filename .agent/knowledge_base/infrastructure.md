# 🏗️ Infrastructure & Docker Standards

> Auto-generated by WB-Agent Scanner → Enriched with actual data

## 🛠️ Tech Stack
- Node.js 22 + Express.js 5 + Sequelize 6 + MySQL 8
- React 18 + Refine 4 + Ant Design 5 + Vite (Admin)
- Next.js 15 + React 19 + Tailwind CSS 3.4 (Client)
- Docker + Docker Compose V2

## 📂 Environment Mapping
- **Local**: `docker-compose.yml` (Hot-reload, Dev-tools)
- **Production**: [Chưa có — cần tạo `docker-compose.prod.yml`]

### Services (5 + network + volume)

| Service | Container | Image/Build |
|---------|-----------|-------------|
| mysql | laplace_mysql | mysql:8.0 |
| server | laplace_server | ./server/Dockerfile |
| phpmyadmin | laplace_phpmyadmin | phpmyadmin/phpmyadmin:latest |
| admin | laplace_admin | ./admin/Dockerfile (target: dev) |
| client | laplace_client | ./client/Dockerfile (target: dev) |

### Port Mapping (Thực tế từ docker-compose.yml)

| Service | External | Internal |
|---------|---------|----------|
| server | 7201 | 8000 |
| admin | 7202 | 7202 |
| client | 7203 | 7203 |
| phpmyadmin | 7204 | 80 |
| mysql | 7205 | 3306 |

> 📌 `.env` và `docker-compose.yml` đã đồng bộ ports 7201-7205

### Volume Mapping

| Host | Container | Service |
|------|-----------|---------|
| `./server` | `/app` | server |
| `./admin` | `/app` | admin |
| `./client` | `/app` | client |
| `mysql_data` (named) | `/var/lib/mysql` | mysql |
| `./server/public/uploads` | `/app/public/uploads` | server |

### Dependencies
- server depends_on mysql (condition: service_healthy)
- phpmyadmin depends_on mysql (condition: service_healthy)

## 🔑 Environment Variables (31)

### Server
- `NODE_ENV`, `APP_NAME`, `APP_URL`
- `DB`, `DB_HOST`, `DB_PORT`, `DB_USERNAME`, `DB_PASSWORD`, `DB_NAME`
- `JWT_SECRET`, `JWT_EXPIRES_IN`
- `MAIL_HOST`, `MAIL_PORT`, `MAIL_USERNAME`, `MAIL_PASSWORD`, `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME`, `MAIL_ENCRYPTION`
- `SOCKET_CORS_ORIGIN`
- `MAX_FILE_SIZE`, `ALLOWED_IMAGE_TYPES`, `ALLOWED_FILE_TYPES`
- `DEFAULT_PAGE_SIZE`, `MAX_PAGE_SIZE`
- `BCRYPT_ROUNDS`, `CORS_ORIGIN`

### Frontend
- `VITE_API_URL` (Admin - Vite)
- `NEXT_PUBLIC_API_URL` (Client - Next.js)

### Port Config (trong .env, chưa đồng bộ docker-compose)
- `SERVER_PORT`, `ADMIN_PORT`, `CLIENT_PORT`

## 🔒 Security Protocol
- Use `.env.example` for all sensitive variables.
- Production images use Alpine/Slim versions.
- MySQL chỉ expose qua Docker internal network (7205 cho debug).
- phpMyAdmin: Root password `rootpassword` (chỉ dev).
- Server healthcheck cho MySQL trước khi start.

## 🐳 Quick Commands

```powershell
# Start
docker compose up -d

# Logs
docker compose logs -f server
docker compose logs -f admin

# Rebuild
docker compose up -d --build server

# Database
docker exec -it laplace_server sh
yarn sequelize-cli db:migrate
yarn sequelize-cli db:seed:all

# Health check
node .agent/scripts/health-check.js
```