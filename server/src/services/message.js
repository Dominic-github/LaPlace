class MessageService {
  createMessage = async (data) => {
    // Implement message creation logic here
  }

  getHistoryByUser = async (userId) => {
    // Implement logic to get message history by user
  }
}

export default new MessageService()
